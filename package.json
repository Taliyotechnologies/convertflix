{
  "name": "convertflix",
  "version": "1.0.0",
  "description": "ConvertFlix - File conversion platform",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
